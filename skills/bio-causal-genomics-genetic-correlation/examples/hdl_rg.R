# HDL global rg: set N0 to the known overlap count; avoid HDL when overlap is substantial.
library(HDL)
# hdl <- HDL.data(ldpath="UKB_array_SVD_eigen100_extraction")
# HDL.gcor(hdl=hdl, sumstats1="trait1.txt", sumstats2="trait2.txt", N0=0, output.file="hdl_rg.txt")
